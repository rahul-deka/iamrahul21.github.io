import React from "react";
// import { Link } from "react-router-dom";

const Home = () => {
    const style = {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        fontSize: "40px"
    };

    return (
        <div style={style}>Revamping going on...</div>
    )
}

export default Home